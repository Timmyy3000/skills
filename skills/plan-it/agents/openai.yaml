interface:
  display_name: "Plan It"
  short_description: "Delegate full or fast implementation planning"
  default_prompt: "Use $plan-it to create this implementation plan through the configured planning worker."
